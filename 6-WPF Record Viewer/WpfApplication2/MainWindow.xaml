<Window x:Class="WpfApplication2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication2"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Stretch" Height="Auto" Width="Auto" Margin="0,2,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="name" Header="Person Name" Binding="{Binding Path=Name}" Width="150"/>
                <DataGridTextColumn x:Name="number_1" Header="Number 1" Binding="{Binding Path=Number1}" Width="120"/>
                <DataGridTextColumn x:Name="number_2" Header="Number 2" Binding="{Binding Path=Number2}" Width="120"/>
                <DataGridTextColumn x:Name="number_3" Header="Number 3" Binding="{Binding Path=Number3}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="button" Content="Tolgahan says start here!" Margin="0,283,0,0" FontSize="16" Click="button_Click"/>
    </Grid>
</Window>
